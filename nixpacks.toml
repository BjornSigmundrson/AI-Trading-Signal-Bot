[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install langchain-anthropic pandas ta ccxt python-dotenv flask psycopg2-binary mcp websocket-client"]

[start]
cmd = "python server.py & python agent.py"
